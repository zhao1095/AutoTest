<?xml version="1.0" encoding="UTF-8" ?>
<suite name="tread" parallel ="methods" thread-count="3">
    <!--
    parallel参数:(是指XML文件中的标签名称)
            methods级别:所有的用例都可以在不同的线程下去执行.

            tests级别:相同的test tag下的用例只能在同一个线程中去执行.
                      不同的test tag下的测试用例可以在不同的线程下去执行.

            classes级别:相同的class tag下的用例在同一个线程中去执行.
                      不同的Class tag下的用例可以在不同线程中执行.
     thread-count参数:代表最大并发线程数.
     xml文件配置这种方式不能指定线程池,只有方法上才可以指定线程池.
    -->
    <test name="test1">
        <classes >

            <class name="com.coures.testng.MultiThread.MultiThreadOnXml"/>
        </classes>
    </test>

    <test name="test2">
        <classes >
            <class name="com.coures.testng.BasicAnnotation"/>
            <class name="com.coures.testng.MultiThread.MultiThreadOnXml"/>
        </classes>
    </test>

</suite>